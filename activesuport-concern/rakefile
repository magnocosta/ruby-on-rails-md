# require 'bundler/gem_tasks'
$stdout.sync = true

desc 'Open Console'
task :console do
  require 'pry'
  require_relative 'example/commentable.rb'
  require_relative 'example/likeable.rb'
  require_relative 'example/post.rb'

  ARGV.clear
  Pry.start
end
